export { authErrorMessage } from "@/lib/auth/errors";
export { calculateProfileCompletion } from "@/lib/auth/profile-completion";
export {
  getSiteUrl,
  profileDisplayName,
  splitFullName,
} from "@/lib/auth/names";
export {
  getCurrentProfile,
  getCurrentUser,
  requireUser,
  type CurrentUser,
} from "@/lib/auth/session";
